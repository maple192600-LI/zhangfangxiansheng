# 24_产品代码开发路线图

## READ WHEN

完成仓库初始化和项目护栏脚本后，开始产品代码开发前读取。

## CORE RULE

产品代码按里程碑推进。每个里程碑必须形成单独任务单和 PR。

未完成当前里程碑的退出条件，不得进入下一个里程碑。

每个里程碑实施前，必须确认对应实施方案包含：

```text
参考依据
架构思路
核心接口
调用链
数据结构
权限边界
测试方法
浏览器验收
禁止事项
```

如果实施方案缺少以上内容，先修改实施方案，不得直接开工。

Agent、Skill、工作流、连接器相关里程碑开工前，必须先读取对应参考项目研究：

```text
docs/41_pi-mono_Agent与Skill技术研究.md
docs/42_Claw_Code权限工具与MCP技术研究.md
docs/43_n8n工作流与Agent_Runtime技术研究.md
docs/44_OpenClaw_Gateway与多Agent技术研究.md
docs/45_Hermes_Agent记忆压缩与自我改进技术研究.md
docs/46_Claude_Code工具契约与兼容边界研究.md
```

## ENTRY CONDITION

开始产品代码开发前必须满足：

```text
GitHub Private 仓库已创建
目标目录已建立
AGENTS.md、PRD、docs/、rules/ 已提交
skills/skill-creator/SKILL.md 已存在
dev/scripts/check_project_guard.py 已实现
项目护栏检查通过
```

## M0：仓库基础与项目护栏

来源：

```text
docs/22_首批开发任务单.md
docs/32_M0实际开工检查清单.md
```

退出条件：

```text
仓库可提交
仓库可推送
首次 PR 已完成
项目护栏脚本可运行
项目护栏脚本失败样例已验证
```

## M1：应用骨架

必须读取：

```text
docs/25_技术栈与工程架构蓝图.md
docs/26_M1应用骨架实施方案.md
```

目标：

```text
建立 FastAPI + Vue + 本地数据目录 + 启动脚本的最小可运行外壳。
```

创建范围：

```text
app/backend/
app/frontend/
app/packaging/
dev/scripts/
```

必须交付：

```text
后端健康检查接口
前端空壳页面
前后端本地启动方式
浏览器能打开首页
项目护栏检查通过
```

不得交付：

```text
业务页面
Agent 功能
Skill 加载
Excel 解析
数据库业务表
```

退出条件：

```text
浏览器打开首页成功
健康检查接口成功
刷新页面不报错
项目护栏检查通过
```

## M2：契约与基础数据模型

必须读取：

```text
docs/25_技术栈与工程架构蓝图.md
docs/27_M2契约与基础数据模型实施方案.md
```

目标：

```text
建立数据库连接、迁移方式、统一 API 响应、字段字典和动态业务对象底座。
```

必须交付：

```text
SQLite 连接
迁移入口
统一响应格式
字段字典表
动态业务对象表
审计日志表
最小 API 契约
```

必须读取：

```text
docs/17_数据模型与字段字典设计规范.md
docs/18_API与前后端契约规范.md
rules/20_数据模型与字段字典.md
rules/21_API与前后端契约.md
```

退出条件：

```text
数据库可初始化
字段字典可读写
API 响应格式统一
后端测试通过
项目护栏检查通过
```

## M3：基础界面框架

必须读取：

```text
docs/25_技术栈与工程架构蓝图.md
docs/28_M3基础界面框架实施方案.md
```

目标：

```text
建立用户能理解的导航、首页、数据中心入口、Agent 入口占位和系统状态区。
```

必须交付：

```text
主布局
导航
首页
数据中心入口
Agent 中心入口
工作流中心入口
系统状态区
中文错误提示框架
```

不得交付：

```text
业务硬编码页面
固定银行模板逻辑
固定报表模板逻辑
假数据冒充真实接口
```

退出条件：

```text
浏览器可打开主要入口
导航切换不报错
刷新后仍能恢复页面
项目护栏检查通过
```

## M4：基础数据初始化闭环

必须读取：

```text
docs/25_技术栈与工程架构蓝图.md
docs/29_M4基础数据初始化闭环实施方案.md
```

目标：

```text
用户上传企业基础数据 Excel，系统生成预览、字段映射草稿，用户确认后写入数据库并可查询。
```

必须交付：

```text
文件上传
Excel 预览
字段识别草稿
字段映射确认
写入暂存区
用户确认入库
基础数据查询
基础数据修改
审计记录
```

必须读取：

```text
rules/01_禁止硬编码与模板适配.md
rules/02_单一事实源与数据联动.md
rules/03_浏览器验收.md
rules/19_样本与测试文件管理.md
```

退出条件：

```text
用户可上传样本
页面可查看预览
用户确认后数据入库
修改主数据后相关显示同步
浏览器验收记录已落文件
项目护栏检查通过
```

## M5：Agent Operation Kernel

目标：

```text
建立可运行的 Agent 工具调用闭环：消息进入 AgentLoop，模型适配器返回 tool_call，权限守卫检查，工具真实执行或阻断，前端展示全过程。
```

必须交付：

```text
Agent 列表
Agent 对话页
页面内召唤 Agent 入口
会话记录
工作区目录绑定
工具调用记录
token 和上下文状态栏
压缩摘要记录
权限边界
FakeModelAdapter
ToolRegistry
filesystem.list
filesystem.read_preview
workspace.write_artifact
未授权路径阻断
```

必须读取：

```text
docs/30_Agent_Runtime架构与实现蓝图.md
docs/31_M5Agent_Operation_Kernel实施方案.md
docs/20_Agent_Runtime与权限边界规范.md
rules/23_Agent_Runtime与权限边界.md
```

不得交付：

```text
Agent 直接写正式数据库
Agent 直接决定金额汇总
无审计的工具调用
只做聊天 UI
用假数据冒充工具执行结果
```

退出条件：

```text
AgentLoop 可运行
授权目录文件可真实读取
未授权目录会被阻断
上下文状态可见
工具调用过程可见
压缩摘要可查看
权限越界会被拦截
浏览器验收记录已落文件
项目护栏检查通过
```

## M6：Skill 加载与管理

目标：

```text
支持全局 Skill、工作区 Skill、skill-creator、兼容导入、启用确认和测试记录。
```

必须交付：

```text
skills/index.json
workspaces/default/skills/index.json
Skill 列表
Skill 草稿
Skill 测试记录
Skill 启用确认
skill-creator 可见
allowed_agents 控制
```

必须读取：

```text
docs/30_Agent_Runtime架构与实现蓝图.md
docs/33_M6Skill加载与管理实施方案.md
docs/21_Skill目录与生命周期规范.md
rules/24_Skill目录与生命周期.md
```

退出条件：

```text
全局 Skill 可识别
工作区 Skill 可识别
未启用草稿不能被正式调用
启用前必须有测试记录
浏览器验收记录已落文件
项目护栏检查通过
```

## M7：工作流配方底座

目标：

```text
建立可视化工作流草稿、步骤卡片、画布查看、样本试跑、启用和版本记录。
```

必须交付：

```text
工作流配方 schema
节点注册表
步骤卡片展示
画布查看
样本试跑
用户确认启用
版本记录
执行记录
失败节点定位
```

必须读取：

```text
docs/30_Agent_Runtime架构与实现蓝图.md
docs/34_M7工作流配方底座实施方案.md
docs/19_工作流配方详细设计规范.md
rules/04_工作流配方.md
rules/22_工作流配方详细设计.md
```

退出条件：

```text
Agent 可生成草稿
用户可查看步骤卡片
画布可查看节点
样本试跑成功后才能启用
启用后由工具层执行
浏览器验收记录已落文件
项目护栏检查通过
```

## M8：通用文件适配与模板学习

目标：

```text
上传未知文件时，Agent 生成解析规则草稿；样本试跑和用户确认后沉淀为可复用规则。
```

必须读取：

```text
docs/35_M8通用文件适配与模板学习实施方案.md
docs/30_Agent_Runtime架构与实现蓝图.md
rules/01_禁止硬编码与模板适配.md
```

必须交付：

```text
已知规则命中
未知文件识别
解析规则草稿
字段映射草稿
样本试跑
规则版本保存
规则回滚
异常说明
```

退出条件：

```text
已知文件可自动识别
未知文件进入学习流程
规则不能硬编码样本路径、列号、文件名
同类文件再次上传可复用规则
浏览器验收记录已落文件
项目护栏检查通过
```

## M9：报表模板与确定性生成

目标：

```text
用户上传报表模板或填写说明，Agent 生成绑定草稿，工具层确定性生成 Excel。
```

必须读取：

```text
docs/36_M9报表模板与确定性生成实施方案.md
docs/19_工作流配方详细设计规范.md
docs/25_技术栈与工程架构蓝图.md
```

必须交付：

```text
报表模板上传
填报说明录入
绑定草稿
数据查询节点
模板填充节点
导出 Excel
生成记录
错误定位
```

退出条件：

```text
模板可预览
绑定草稿可确认
导出文件可打开或解析
关键字段和金额可复查
浏览器验收记录已落文件
项目护栏检查通过
```

## M10：默认业务域页面

目标：

```text
补齐资金、OCR、贷款、预算、文件仓库、规则与自动化、连接器、系统管理等默认入口和最小闭环。
```

必须读取：

```text
docs/37_M10默认业务域页面实施方案.md
docs/18_API与前后端契约规范.md
docs/28_M3基础界面框架实施方案.md
```

规则：

```text
每个业务域单独任务单
每个业务域单独 PR
没有数据闭环的页面不得声明完成
没有浏览器验收记录的页面不得声明完成
```

退出条件：

```text
默认入口可访问
页面动作有 API 契约
页面数据来自后端
错误提示中文可读
浏览器验收记录已落文件
项目护栏检查通过
```

## M11：连接器与外部操作接口

目标：

```text
提供 CLI、API、MCP 适配边界，让 AI Coding 工具和企业系统可按权限访问能力。
```

必须读取：

```text
docs/38_M11连接器与外部操作接口实施方案.md
docs/20_Agent_Runtime与权限边界规范.md
docs/18_API与前后端契约规范.md
```

必须交付：

```text
本地 API
CLI 命令入口
连接器权限配置
读写能力边界
审计记录
外部调用示例
```

退出条件：

```text
CLI 可读取状态
CLI 可触发受控任务
API 权限可配置
外部写入必须走确认或受控节点
审计记录可查
项目护栏检查通过
```

## M12：打包与交付验收

目标：

```text
形成用户可运行的本地应用交付包。
```

必须读取：

```text
docs/39_M12打包与交付验收实施方案.md
docs/01_项目文件地图与交付边界.md
docs/05_开发产物与清理规范.md
```

必须交付：

```text
本地启动方式
前端构建
后端挂载
打包脚本
清理脚本
交付白名单
回归验收记录
```

退出条件：

```text
用户无需命令行即可启动
交付包不包含 dev/
交付包不包含 docs/
交付包不包含测试样本
交付包不包含运行数据库
浏览器完整回归通过
项目护栏检查通过
```

## GLOBAL EXIT RULE

每个里程碑完成时必须输出：

```text
任务单：
PR：
修改文件：
后端自检：
浏览器验收记录：
项目护栏检查：
用户检查路径：
未完成项：
进入下一里程碑判断：
```

任一项缺失，不得进入下一里程碑。

