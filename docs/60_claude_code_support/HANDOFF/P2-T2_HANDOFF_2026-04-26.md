# P2-T2 交班单：隐私三档复核

## 完成范围

- 本任务卡与 Phase 7 已交付内容重叠，本轮执行方式为复核与补交班记录。
- `ai_configs.privacy_mode` 字段已生效：
  - ORM：`backend/db/tables.py::AIConfig.privacy_mode`
  - API 入参：`backend/api/ai_config.py`
  - 服务校验：`backend/services/ai_config_service.py` 调用 `validate_privacy_mode`
- UI 切换已生效：
  - `frontend/src/views/AIConfig.vue` 在列表显示隐私档位。
  - 新增/编辑 AI 配置时可选择 `standard` / `strict` / `offline`。
- 样本脱敏器已生效：
  - `standard`：保留列头与脱敏样本，金额按千位取整，长账号/编码数字掩码，名称只保留首字。
  - `strict`：只保留列头，数据行为空。
  - `offline`：在 Fund Agent skill 执行前阻断，并记录 `ai_call_logs.error_code='PRIVACY_OFFLINE'`。
- 即时生效路径已确认：
  - `backend/agents/fund/harness.py` 每次 `run_skill` 都读取当前 active/default `AIConfig.privacy_mode`，不依赖进程启动时缓存。

## 新增文件

- `docs/60_claude_code_support/HANDOFF/P2-T2_HANDOFF_2026-04-26.md`

## 修改文件

- `docs/20_execution/00_v3_execution_order.md`

## 验收证据

```powershell
pytest tests\fund\test_phase7_privacy.py -q
4 passed, 1 warning in 0.87s
```

```powershell
pytest tests\e2e\test_full_flow.py::test_privacy_offline_blocks_skill_before_artifact -q
1 passed, 1 warning in 1.10s
```

```powershell
npm run build
✓ built in 625ms
# Vite chunk-size warning remains, build exit code 0
```

## 对照结论

| P2-T2 交付物 | 状态 | 证据 |
|---|---|---|
| `ai_configs.privacy_mode` 字段生效 | 已完成 | ORM / schema / API / service 均接入 |
| UI 切换界面 + 即时生效 | 已完成 | AIConfig 页面可编辑；harness 每次 skill 调用读取 DB 当前配置 |
| 样本脱敏器 standard / strict | 已完成 | `test_mask_for_llm_privacy_modes` 覆盖 |
| offline 阻断 | 已完成 | fund 单测 + E2E 均覆盖，日志记录 `PRIVACY_OFFLINE` |

## 已知风险

- 当前 5 个 skill 仍以本地确定性实现为主；若后续接入真实 LLM provider，必须继续只使用 `privacy_preview`，不能把原始样本行绕过 harness 发送。
- `ai_call_logs` 当前记录调用大小、状态和错误码，不保存 prompt 明文；因此隐私回归以 `mask_for_llm` 输出和 offline 阻断作为自动化证据。
- API inventory 红项仍按 P6/P7/P8/P2-T1 口径记录，不属于本任务卡阻断项。

## 是否达到下一任务卡进入条件

YES。

理由：P2-T2 三项交付物已在 P7 完成，本轮复核了后端字段、服务校验、UI 切换、harness 即时读取、standard/strict/offline 自动化测试与前端构建。
