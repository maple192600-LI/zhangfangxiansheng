# P2-T1 交班单：异常中心独立页

## 完成范围

- 后端补齐契约端点 35/36/37：
  - `GET /api/events/pending`
  - `PUT /api/events/{event_id}/resolve`
  - `PUT /api/events/{event_id}/void`
- 新增异常中心服务：
  - 列出 `fund_events.state in ('待确认', '异常')` 的待处理流水。
  - 支持按状态、摘要/对方关键词筛选。
  - 支持修正部分 CANONICAL_12 字段后标记为 `正常`。
  - 支持将流水标记为 `已作废`。
  - 处理与作废均写入 `operation_logs`，module=`exception_center`。
- 前端补齐独立页：
  - `frontend/src/views/ExceptionCenter.vue`
  - 原系统设置下“异常中心”的两个占位路由均指向该页面。
  - 页面包含状态筛选、待确认/异常计数、异常列表、修正表单、作废操作。

## 新增文件

- `backend/api/events.py`
- `backend/services/exception_center_service.py`
- `frontend/src/api/events.js`
- `frontend/src/views/ExceptionCenter.vue`
- `tests/e2e/test_exception_center.py`
- `docs/60_claude_code_support/HANDOFF/P2-T1_HANDOFF_2026-04-26.md`

## 修改文件

- `backend/main.py`
- `frontend/src/router/index.js`
- `tests/e2e/conftest.py`
- `docs/20_execution/00_v3_execution_order.md`

## 验收证据

```powershell
pytest tests\e2e\test_exception_center.py -q
2 passed, 1 warning in 1.39s
```

```powershell
pytest tests\e2e -q
7 passed, 1 warning in 5.23s
```

```powershell
pytest tests\fund -q
225 passed, 1 warning in 7.07s
```

```powershell
pytest tests\backend\core tests\db tests\scripts -q
23 passed, 1 warning in 4.24s
```

```powershell
python -m compileall backend\api\events.py backend\services\exception_center_service.py backend\main.py tests\e2e\conftest.py tests\e2e\test_exception_center.py -q
# exit code 0
```

```powershell
npm run build
✓ built in 588ms
# Vite chunk-size warning remains, build exit code 0
```

```powershell
python tools\guards\check_contract_hash.py
[OK] contracts.lock 校验通过（1 个契约文件）
```

```powershell
python tools\guards\check_canonical_schema.py
[OK] fund_events 12 列按 §C1 契约序连续出现（+5 元数据列：['id', 'batch_id', 'parser_artifact_id', 'created_at', 'updated_at']）
```

```powershell
python tools\guards\check_primitives_whitelist.py
[OK] 基元库白名单扫描通过（13 个文件）
```

```powershell
python tools\guards\check_placeholder_binding.py
[OK] 占位符绑定校验通过（12 个 Rule 各自覆盖 18/18）
```

```powershell
python tools\guards\check_api_inventory.py
[FAIL] API 端点 110 > 上限 42（§C7 冻结为 42）
# 本任务补齐契约 35/36/37 三个端点，延续 P6/P7/P8 已记录的 API inventory 红项口径。
```

## 已知风险

- 异常中心当前只处理 `fund_events` 的状态流转，不新增“异常原因”独立表；原因与备注进入 `operation_logs.detail_json`。
- 前端使用 `window.prompt` 收集作废原因，满足 P2-T1 最小闭环；更细的确认弹窗可后续统一 UI 组件时再收敛。
- API inventory 从 107/42 变为 110/42，因为契约中原本列出的异常中心 35/36/37 端点现在真实存在；总体端点治理债务仍未解决。

## 是否达到下一任务卡进入条件

YES。

理由：P2-T1 要求的独立页、端点 35/36/37、操作日志记录已完成；相关 E2E、完整 P8 E2E、Fund Agent 回归、核心测试、前端构建和阻断型守护均通过。
